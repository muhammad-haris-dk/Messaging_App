import { TextField } from "@mui/material";
import React from "react";

const Input = (props) => {
  return (
    <div>
      <input
        {...props}
      />
    </div>
  );
};

export default Input;
